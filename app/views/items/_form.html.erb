<%= form_for([@list, @list.items.build]) do |f| %>
  <p>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= 'notes'  %><br/>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
